# Deprecated Backups UI Archive

This folder is a source archive only. It is intentionally outside `app/lib`
and `app/test`, so the Flutter app and normal test suite do not compile it.

Archived files:

- `backups_screen.dart`
- `backup_history_state.dart`
- `backups_render_capture_test.dart`

The old route and sidebar entry were removed from the app. Keep these files only
as historical reference for the retired manual/automatic restore snapshot UI.
